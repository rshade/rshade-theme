# Normalize line endings to LF for all text files so the vendored submodule
# stays byte-identical across Windows / macOS / Linux consumers.
* text=auto eol=lf

*.css text eol=lf
*.json text eol=lf
*.md text eol=lf
